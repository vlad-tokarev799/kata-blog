import styled from 'styled-components';

export type ProfilePageStyles = object;

export const StyledProfilePage = styled.div<ProfilePageStyles>`
  .success-message {
    position: fixed;
    right: 15px;
    bottom: 15px;
    width: 300px;
  }
`;
